{{#each flashMessages.queue as |flash|}}
  {{#flash-message flash=flash}}
    {{flash.message}}
  {{/flash-message}}
{{/each}}

{{nav-bar logoutTransition=(action "logoutTransition")}}

<div class="body-wrapper">
  {{outlet}}
</div>
