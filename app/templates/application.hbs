{{#each flashMessages.queue as |flash|}}
  {{#flash-message flash=flash}}
    {{flash.message}}
  {{/flash-message}}
{{/each}}

{{nav-bar currentUser=session.currentUser logout=(action "logout")}}

<div class="body-wrapper">
  {{outlet}}
</div>
